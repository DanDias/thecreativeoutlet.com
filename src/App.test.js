import { queryAllByAltText, render, screen } from '@testing-library/react';

/* test('Clicking on a slider radio button changes slider',()=> {
  expect(render(""));
}) */
test('REALLY BASIC CHECK that is useless mostly', () => {
  expect("a").toBe("a");
})